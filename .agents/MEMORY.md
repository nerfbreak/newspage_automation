# AI Project Memory (Optimize Newspage)

This file acts as the "Distributed Project Memory" for AI agents. It tracks architectural decisions, recent changes, and known states to prevent hallucinations and maintain a single source of truth across all AI sessions.

## Current State Summary
- **Frontend**: Streamlit (Pages: Dashboard, Inventory Adjustment, Sales Extraction, Promotion Comparison, Stock Mutation, Clearance Stock, Initial Stock).
- **Backend/DB**: Supabase (PostgreSQL) for user auth, vault, and system config. `cryptography` for AES-256 encryption.
- **Automation Engine**: Playwright (`playwright_engine.py`) handling interactions with the Newspage portal.

---

## ðŸ”’ LOCKED DESIGN SYSTEM â€” Neo-Brutalism (PERMANENT, DO NOT OVERRIDE)

> **CRITICAL RULE**: This project uses **Neo-Brutalism** as its one and only design language. This is a **permanent, locked decision**. ANY future UI work â€” new pages, new components, new modals, new alerts, new widgets â€” MUST strictly follow this design system. Do NOT introduce Material Design, Glassmorphism, Neumorphism, Bootstrap, TailwindCSS default utilities, or any other aesthetic system that contradicts the rules below.

### Design Tokens (The Law)
| Token | Value | Notes |
|---|---|---|
| **Primary Color** | `#0068C9` | Streamlit Blue â€” buttons, links, accents |
| **Accent Yellow** | `#FFDE59` | Highlight, badge background |
| **Accent Red** | `#E63946` | Destructive actions (delete, terminate, confirm danger) |
| **Dark / Text / Border** | `#0F172A` | Near-black â€” ALL borders, ALL shadows, ALL key text |
| **Page Background** | `#dbeafe` | Light blue canvas (global) |
| **Card Background** | `#FFFFFF` | Pure white for all cards/containers |
| **Subtle Surface** | `#F1F5F9` | Cancel buttons, secondary areas |
| **Container Border** | `3px solid #0F172A` | ALWAYS â€” zero exceptions on containers/cards |
| **Button Border** | `2px solid #0F172A` | For buttons specifically |
| **Border Radius** | `0px` | ALWAYS sharp corners â€” never rounded |
| **Box Shadow** | `6px 6px 0px 0px #0F172A` | Flat, NO blur â€” the Neo-Brutalism signature |
| **Hover Shadow** | `4px 4px 0px 0px #0F172A` | Reduce on hover |
| **Active Shadow** | `2px 2px 0px 0px #0F172A` | Reduce further on click |
| **Hover Transform** | `translate(2px, 2px)` | Physical press feel â€” always paired with shadow reduction |
| **Font (Body)** | `Source Sans 3`, `Source Sans Pro` | Google Fonts |
| **Font (Terminal/Code)** | `Source Code Pro` | Monospace â€” terminal logs only |
| **Font Weight (Labels)** | `800` | All caps labels, uppercase badges |
| **Font Weight (Headings)** | `900` | Maximum impact titles |
| **Letter Spacing (Labels)** | `0.05em â€“ 0.08em` | Uppercase badge labels |

### Standard Component Dimensions
| Component | Rule |
|---|---|
| **Metric Cards** | Height: `125px`, Padding: `20px 24px` |
| **Standard Buttons** | Min-Height: `44px`, Border: `2px solid #0F172A`, Shadow: `6px 6px 0px 0px #0F172A` |
| **Containers / Cards** | Border: `3px solid #0F172A`, Shadow: `6px 6px 0px 0px #0F172A`, Radius: `0px` |
| **Terminal Log Box** | Border: `4px solid #0F172A`, Shadow: `8px 8px 0px 0px #0F172A`, Height: `350px` |
| **Modal Overlay Backdrop** | `rgba(15,23,42,0.7)` + `backdrop-filter: blur(4px)` (backdrop-filter on overlay ONLY, not content cards) |

### 10 Rules That CANNOT Be Violated
1. **NO glassmorphism** â€” no `backdrop-filter` on content cards, no `rgba` semi-transparent backgrounds on main UI elements.
2. **NO border-radius** on any card, button, input, or container â€” always `0px`.
3. **NO emoji in UI** â€” use Streamlit Material Icons (`:material/icon_name:`) only.
4. **NO box-shadow blur** â€” always `box-shadow: X Y 0px 0px #0F172A` (blur and spread values always `0px`).
5. **NO inline style drift** â€” all values must match the design token table above exactly.
6. **NO Streamlit default header/sidebar** â€” always hidden via `display: none !important` in CSS.
7. **CSS sibling targeting** must use the `:has()` selector pattern for Streamlit element targeting â€” never `~` alone.
8. **Safe HTML** â€” always run dynamic user-facing text through `html.escape()` before injecting into `st.markdown`.
9. **Background rule** â€” global page bg is always `#dbeafe`; all card/container bg is always `#FFFFFF`.
10. **Modals are CSS-only** â€” use `<input type="checkbox">` toggle pattern. Never use JavaScript for modal open/close.

---

## ðŸ”’ Locked Features & Code Freeze
- **Frozen Modules**: **Stock Mutation**, **Inventory Adjustment**, **Sales Extraction**, **Promotion Comparison**, **Clearance Stock**, **Initial Stock**, and **Credential Auto-Encryption**.
- **Rule**: All core execution flow, Playwright steps, Supabase connections, and credential handling for these features are locked. Any future development or new features must build on top of or alongside these modules without modifying their verified core logics.
- **Unlock Password**: If modification to the frozen logic is explicitly requested, you must verify the password `"Dama"` in the chat before doing any changes.
- **Changelog Restriction**: When updating `CHANGELOG.md`, only user-facing Features (`### Added`) and Bug Fixes (`### Fixed`) must be recorded.

---

## Changelog & Decisions

- **2026-06-24**: Reverted the Bento Box UI as it broke Streamlit's layout engine. Implemented a **Streamlit-Native Premium UI** instead: set elegant colors in `config.toml`, stripped destructive CSS overrides from `style.css` and `login.css`, and updated `utils.py` to use flat, safe, inline-styled HTML components.
- **2026-06-24**: Rolled back UI to original state per user request. Enabled global font antialiasing.
- **2026-06-25**: Critical security fixes â€” added `html.escape()` to all HTML injection points in `utils.py` (XSS prevention), enabled CORS in `config.toml`, pinned all dependency versions in `requirements.txt`, moved orphaned `refactor.py` to `scripts/`, added `EXCLUDE_PREFIX` constant to `database.py`, fixed misleading docstring in `database.py`, removed dead `style_status()` function from `utils.py`.
- **2026-06-26**: Fixed Stock Mutation quantity execution bug: added a fallback to the `Qty` column when `PAC`, `CAR`, and `EA` columns are missing, ensuring quantities are correctly written to the EA input in the Newspage portal instead of injecting empty/zero values. Pre-initialized Status and Keterangan columns in dataframes to avoid visual glitches.
- **2026-06-26**: Resolved "Decryption error" issue: restored missing local `.streamlit/secrets.toml` from backup to enable decryption of distributor passwords locally, and identified that `MASTER_KEY` needs to be set to `7Cm4lTUOm1-DPgwHRE28ZCZ28b33KDMVV_8Ug_AxiWM=` in the deployment env/secrets.
- **2026-06-26**: Implemented auto-encryption feature for plain-text passwords: if a distributor password stored in Supabase fails decryption and is detected as plain text (does not start with 'gAAAA'), it will be automatically encrypted and written back to Supabase on first fetch, enabling a seamless workflow for registering new distributors.
- **2026-06-27**: Documented design guidelines based on the user's positive feedback. The clean layout spacing, vertical left-bordered header highlights, soft-blue disclaimer wrapper, and escaped entity handling are established as the official UI/UX standard.
- **2026-06-27**: Resolved visual container collision on Page 4 (Stock Mutation) by extracting the execution block (dual tables, progress indicators, terminal logs, and success status indicators) outside the input `st.container(border=True)` block, letting them render naturally at the page level without cramped layout borders.
- **2026-06-27**: Established "Locked Features & Code Freeze" rule in both `AGENTS.md` and `MEMORY.md` per user request to freeze all existing core features and prevent future AI assistants from refactoring or modifying verified logic when adding new modules.
- **2026-06-27**: Added a Password Verification check ("Dama") to the logic freeze rules. Any request to override or modify locked modules must be verified by this password in the chat before execution.
- **2026-06-27**: Restricted CHANGELOG.md to only track user-facing Features (Added) and Bug Fixes (Fixed) per user's request. Added this restriction to local locked rules.
- **2026-06-27**: Fixed frozen progress bar issue in Stock Mutation by updating `run_execution_manual` to accept `progress_placeholder` and dynamically increment progress values, and passed `prog_a_ph`/`prog_b_ph` from `run_mutasi_execution` (Unlocked via password verification).
- **2026-06-27**: Consolidated the two separate success/error status boxes into a single merged status box on the Stock Mutation page to improve visual aesthetics and reduce clutter (Unlocked via password verification).
- **2026-06-27**: Updated the Streamlit login UI expander in `app.py` to dynamically load and display the contents of `CHANGELOG.md` instead of using a hardcoded text list, ensuring any future updates to `CHANGELOG.md` are instantly rendered in the app.
- **2026-06-27**: Set the cat-with-laptop image (`static/favicon.png`) as the official global webpage favicon/icon for the Streamlit application in `app.py` page configuration.
- **2026-06-27**: Translated CHANGELOG.md to Indonesian (using "Fitur Baru" and "Perbaikan Bug" headers) and updated the dynamic parser in `app.py` to strip out metadata like `# Changelog` and `## [Unreleased]`, rendering only user-facing additions and fixes.
- **2026-06-27**: Rewrote CHANGELOG.md in a simple, jargon-free Indonesian format focusing strictly on user-facing features and feature improvements, removing technical developer terms like syntax errors, attribute errors, and file compilation details.
- **2026-06-27**: Reverted CHANGELOG.md back to English (using Added/Fixed headers), keeping it user-friendly and simple, and removed the auto-encryption and app icon entries per user request.
- **2026-06-27**: Implemented a comprehensive Operational Report & Analytics section on the main Dashboard page (0_dashboard.py). Added cached log fetching from Supabase, a period filter selector, KPI cards, interactive sync/extraction trend charts, and custom styled HTML tables showing recent execution logs under tabs.
- **2026-06-27**: Updated the footer disclaimer text in `utils.py` to be more professional, and explicitly stated that the application is unofficial and has no official affiliation with Reckitt, Accenture, or the Newspage platform.
- **2026-06-27**: Fixed HTML rendering bug in dashboard recent execution tables by wrapping them in `clean_html()`. Updated dashboard metric card and distributor chart to display count of unique SKUs adjusted instead of quantity volumes.
- **2026-06-27**: Replaced deprecated `use_container_width=True` with `width="stretch"` on dashboard charts to resolve Streamlit deprecation warnings.
- **2026-06-27**: Replaced deprecated `use_container_width=True` with `width="stretch"` on buttons across all locked pages (unlocked via "Dama" password verification) to resolve Streamlit deprecation warnings.
- **2026-06-27**: Simplified the Dashboard Activity Report: removed all charts (area chart, bar chart, status distribution) and separate tabbed tables. Replaced with a single unified "Execution History" table showing 5 columns: Timestamp, Distributor, Module (color-coded badges), Status, and Run By. Merges data from `adjustment_logs` and `extraction_history` tables. Module detection uses qty format to distinguish Inventory Adjustment vs Stock Mutation.
- **2026-06-27**: Fixed "Run By" column on Dashboard to show Streamlit login username instead of NP bot account code. Added optional `run_by` parameter to `log_adjustment()` in `database.py`. Updated `run_execution`, `run_execution_manual`, `run_mutasi_execution` in `playwright_engine.py` and all call sites in pages (1, 4, 5, 6) to pass `st.session_state.current_user`. Old log records without `run_by` fall back to showing `np_user`. Also requires adding a `run_by TEXT` column to the `adjustment_logs` table in Supabase (user action required).
- **2026-06-27**: Added "Today" option to dashboard period filter. Renamed "Execution History" header to "Log History".
- **2026-06-30**: Fixed Dashboard connection ping test for Superuser credentials: replaced raw `requests` HTTP implementation with a headless Playwright subprocess polling loop to natively handle the ASP.NET client-side RSA encryption and `SYS_ASCX_btnContinue` session interceptor, ensuring accurate connection verification.
- **2026-06-27**: Implemented File Upload Tracking on Dashboard. Added `log_uploaded_file()` to `database.py` storing base64-encoded Excel/CSV files in `uploaded_files` Supabase table. Updated `1_inventory_adjustment.py` to store the uploaded distributor file before execution (Auto Compare mode only). Dashboard `load_historical_logs()` now fetches `uploaded_files` and builds a 5-minute bucket lookup to match files to log rows. Log History table now has a 6th "File Uploaded" column rendering an inline base64 download anchor link. Requires user to create `uploaded_files` table in Supabase.
- **2026-06-28**: Restructured the Dashboard layout (`0_dashboard.py`) into a more hierarchical "Home Screen" style since the sidebar is hidden. Moved System Health and KPI Metrics to the top (Hero Section), placed the Navigation Hub (App Launcher) in the middle, and anchored the Activity Report logs at the bottom.
- **2026-06-28**: Executed a "Command Center" UI Overhaul for `0_dashboard.py`. Replaced the standard Streamlit grid with an asymmetrical 70/30 split. The left side (70%) contains a 3x2 grid of modern App Launcher cards with colored SVG icons. The right side (30%) contains live pulse indicators and a Vertical Timeline Activity Feed. The design strictly adheres to the solid-color/no-glassmorphism constraints.
- **2026-06-28**: Refactored CSS styling to ensure consistent premium design across all pages. Extracted inline `<style>` blocks (buttons, segmented controls, pulse animations) from `0_dashboard.py`, `1_inventory_adjustment.py`, and `utils.py`, moving them into the global `static/style.css`. This upgraded the visual aesthetic of all internal modules without modifying any core Playwright logic.
- **2026-06-28**: Upgraded the inner module headers in `utils.py` (`render_header`) to use a modern, gradient Hero Banner with custom SVG icons and subtitles that match the Command Center design language.
- **2026-06-28**: Fixed a bug in `0_dashboard.py` where the Recent Activity timeline incorrectly labeled Inventory Extraction logs as "Sales". Corrected the hardcoded module label to "Inventory" to match the actual source of the `extraction_history` data.
- **2026-06-28**: Restored the original full-width underline aesthetic for inner module section headers by modifying `.section-header-underline` in `style.css` to use `display: block; width: 100%;` per user request.
- **2026-06-28**: Fixed a bug where Sales Extraction executions were not being logged to the `extraction_history` table and Dashboard Recent Activity. Updated `database.py` to support an optional `status` parameter, and modified `run_sales_extract` in `playwright_engine.py` to log it. Also updated `0_dashboard.py` to differentiate Sales vs Inventory based on this status suffix.
- **2026-06-28**: Locked the height of the Dashboard's "Recent Activity" container to exactly `544px` to perfectly align with the 3x2 Application Modules grid on the left.
- **2026-06-28**: Implemented a secret "Element Crawler" module (`pages/7_element_crawler.py`) for scraping DOM elements from the Accenture portal. Hidden from the standard sidebar via CSS (`static/style.css`). Accessed via a 3-click Easter Egg on the "Automation Tool" header text on the dashboard (`utils.py`). Added `run_element_crawler` logic to `playwright_engine.py`.
## Changelog & Decisions

- **2026-06-24**: Initialized AI Project Memory system. Established `AGENTS.md` to force all future AI interactions to read and write to this `MEMORY.md` file.
- **[Archived Decision]**: Implemented 5 login attempts lockout and 1-hour session timeout in `app.py`.
- **[Archived Decision]**: Implemented AES-256 Fernet encryption for `distributor_vault` credentials.
- **2026-06-24**: Reverted the Bento Box UI as it broke Streamlit's layout engine. Implemented a **Streamlit-Native Premium UI** instead: set elegant colors in `config.toml`, stripped destructive CSS overrides from `style.css` and `login.css`, and updated `utils.py` to use flat, safe, inline-styled HTML components.
- **2026-06-24**: Rolled back UI to original state per user request. Enabled global font antialiasing.
- **2026-06-25**: Critical security fixes â€” added `html.escape()` to all HTML injection points in `utils.py` (XSS prevention), enabled CORS in `config.toml`, pinned all dependency versions in `requirements.txt`, moved orphaned `refactor.py` to `scripts/`, added `EXCLUDE_PREFIX` constant to `database.py`, fixed misleading docstring in `database.py`, removed dead `style_status()` function from `utils.py`.
- **2026-06-26**: Fixed Stock Mutation quantity execution bug: added a fallback to the `Qty` column when `PAC`, `CAR`, and `EA` columns are missing, ensuring quantities are correctly written to the EA input in the Newspage portal instead of injecting empty/zero values. Pre-initialized Status and Keterangan columns in dataframes to avoid visual glitches.
- **2026-06-26**: Resolved "Decryption error" issue: restored missing local `.streamlit/secrets.toml` from backup to enable decryption of distributor passwords locally, and identified that `MASTER_KEY` needs to be set to `7Cm4lTUOm1-DPgwHRE28ZCZ28b33KDMVV_8Ug_AxiWM=` in the deployment env/secrets.
- **2026-06-26**: Implemented auto-encryption feature for plain-text passwords: if a distributor password stored in Supabase fails decryption and is detected as plain text (does not start with 'gAAAA'), it will be automatically encrypted and written back to Supabase on first fetch, enabling a seamless workflow for registering new distributors.
- **2026-06-27**: Documented design guidelines based on the user's positive feedback. The clean layout spacing, vertical left-bordered header highlights, soft-blue disclaimer wrapper, and escaped entity handling are established as the official UI/UX standard.
- **2026-06-27**: Resolved visual container collision on Page 4 (Stock Mutation) by extracting the execution block (dual tables, progress indicators, terminal logs, and success status indicators) outside the input `st.container(border=True)` block, letting them render naturally at the page level without cramped layout borders.
- **2026-06-27**: Established "Locked Features & Code Freeze" rule in both `AGENTS.md` and `MEMORY.md` per user request to freeze all existing core features and prevent future AI assistants from refactoring or modifying verified logic when adding new modules.
- **2026-06-27**: Added a Password Verification check ("Dama") to the logic freeze rules. Any request to override or modify locked modules must be verified by this password in the chat before execution.
- **2026-06-27**: Restricted CHANGELOG.md to only track user-facing Features (Added) and Bug Fixes (Fixed) per user's request. Added this restriction to local locked rules.
- **2026-06-27**: Fixed frozen progress bar issue in Stock Mutation by updating `run_execution_manual` to accept `progress_placeholder` and dynamically increment progress values, and passed `prog_a_ph`/`prog_b_ph` from `run_mutasi_execution` (Unlocked via password verification).
- **2026-06-27**: Consolidated the two separate success/error status boxes into a single merged status box on the Stock Mutation page to improve visual aesthetics and reduce clutter (Unlocked via password verification).
- **2026-06-27**: Updated the Streamlit login UI expander in `app.py` to dynamically load and display the contents of `CHANGELOG.md` instead of using a hardcoded text list, ensuring any future updates to `CHANGELOG.md` are instantly rendered in the app.
- **2026-06-27**: Set the cat-with-laptop image (`static/favicon.png`) as the official global webpage favicon/icon for the Streamlit application in `app.py` page configuration.
- **2026-06-27**: Translated CHANGELOG.md to Indonesian (using "Fitur Baru" and "Perbaikan Bug" headers) and updated the dynamic parser in `app.py` to strip out metadata like `# Changelog` and `## [Unreleased]`, rendering only user-facing additions and fixes.
- **2026-06-27**: Rewrote CHANGELOG.md in a simple, jargon-free Indonesian format focusing strictly on user-facing features and feature improvements, removing technical developer terms like syntax errors, attribute errors, and file compilation details.
- **2026-06-27**: Reverted CHANGELOG.md back to English (using Added/Fixed headers), keeping it user-friendly and simple, and removed the auto-encryption and app icon entries per user request.
- **2026-06-27**: Implemented a comprehensive Operational Report & Analytics section on the main Dashboard page (0_dashboard.py). Added cached log fetching from Supabase, a period filter selector, KPI cards, interactive sync/extraction trend charts, and custom styled HTML tables showing recent execution logs under tabs.
- **2026-06-27**: Updated the footer disclaimer text in `utils.py` to be more professional, and explicitly stated that the application is unofficial and has no official affiliation with Reckitt, Accenture, or the Newspage platform.
- **2026-06-27**: Fixed HTML rendering bug in dashboard recent execution tables by wrapping them in `clean_html()`. Updated dashboard metric card and distributor chart to display count of unique SKUs adjusted instead of quantity volumes.
- **2026-06-27**: Replaced deprecated `use_container_width=True` with `width="stretch"` on dashboard charts to resolve Streamlit deprecation warnings.
- **2026-06-27**: Replaced deprecated `use_container_width=True` with `width="stretch"` on buttons across all locked pages (unlocked via "Dama" password verification) to resolve Streamlit deprecation warnings.
- **2026-06-27**: Simplified the Dashboard Activity Report: removed all charts (area chart, bar chart, status distribution) and separate tabbed tables. Replaced with a single unified "Execution History" table showing 5 columns: Timestamp, Distributor, Module (color-coded badges), Status, and Run By. Merges data from `adjustment_logs` and `extraction_history` tables. Module detection uses qty format to distinguish Inventory Adjustment vs Stock Mutation.
- **2026-06-27**: Fixed "Run By" column on Dashboard to show Streamlit login username instead of NP bot account code. Added optional `run_by` parameter to `log_adjustment()` in `database.py`. Updated `run_execution`, `run_execution_manual`, `run_mutasi_execution` in `playwright_engine.py` and all call sites in pages (1, 4, 5, 6) to pass `st.session_state.current_user`. Old log records without `run_by` fall back to showing `np_user`. Also requires adding a `run_by TEXT` column to the `adjustment_logs` table in Supabase (user action required).
- **2026-06-27**: Added "Today" option to dashboard period filter. Renamed "Execution History" header to "Log History".
- **2026-06-30**: Fixed Dashboard connection ping test for Superuser credentials: replaced raw `requests` HTTP implementation with a headless Playwright subprocess polling loop to natively handle the ASP.NET client-side RSA encryption and `SYS_ASCX_btnContinue` session interceptor, ensuring accurate connection verification.
- **2026-06-27**: Implemented File Upload Tracking on Dashboard. Added `log_uploaded_file()` to `database.py` storing base64-encoded Excel/CSV files in `uploaded_files` Supabase table. Updated `1_inventory_adjustment.py` to store the uploaded distributor file before execution (Auto Compare mode only). Dashboard `load_historical_logs()` now fetches `uploaded_files` and builds a 5-minute bucket lookup to match files to log rows. Log History table now has a 6th "File Uploaded" column rendering an inline base64 download anchor link. Requires user to create `uploaded_files` table in Supabase.
- **2026-06-28**: Restructured the Dashboard layout (`0_dashboard.py`) into a more hierarchical "Home Screen" style since the sidebar is hidden. Moved System Health and KPI Metrics to the top (Hero Section), placed the Navigation Hub (App Launcher) in the middle, and anchored the Activity Report logs at the bottom.
- **2026-06-28**: Executed a "Command Center" UI Overhaul for `0_dashboard.py`. Replaced the standard Streamlit grid with an asymmetrical 70/30 split. The left side (70%) contains a 3x2 grid of modern App Launcher cards with colored SVG icons. The right side (30%) contains live pulse indicators and a Vertical Timeline Activity Feed. The design strictly adheres to the solid-color/no-glassmorphism constraints.
- **2026-06-28**: Refactored CSS styling to ensure consistent premium design across all pages. Extracted inline `<style>` blocks (buttons, segmented controls, pulse animations) from `0_dashboard.py`, `1_inventory_adjustment.py`, and `utils.py`, moving them into the global `static/style.css`. This upgraded the visual aesthetic of all internal modules without modifying any core Playwright logic.
- **2026-06-28**: Upgraded the inner module headers in `utils.py` (`render_header`) to use a modern, gradient Hero Banner with custom SVG icons and subtitles that match the Command Center design language.
- **2026-06-28**: Fixed a bug in `0_dashboard.py` where the Recent Activity timeline incorrectly labeled Inventory Extraction logs as "Sales". Corrected the hardcoded module label to "Inventory" to match the actual source of the `extraction_history` data.
- **2026-06-28**: Restored the original full-width underline aesthetic for inner module section headers by modifying `.section-header-underline` in `style.css` to use `display: block; width: 100%;` per user request.
- **2026-06-28**: Fixed a bug where Sales Extraction executions were not being logged to the `extraction_history` table and Dashboard Recent Activity. Updated `database.py` to support an optional `status` parameter, and modified `run_sales_extract` in `playwright_engine.py` to log it. Also updated `0_dashboard.py` to differentiate Sales vs Inventory based on this status suffix.
- **2026-06-28**: Locked the height of the Dashboard's "Recent Activity" container to exactly `544px` to perfectly align with the 3x2 Application Modules grid on the left.
- **2026-06-28**: **Fixed `_login` Race Condition (Dama Unlocked):** Replaced static 5s wait with dynamic 60s polling loop to handle slow Newspage servers showing "Same User Already Logged On" popup late.
- **2026-06-28**: **Fixed Dashboard Duplicate Logs:** Added `drop_duplicates` grouped by minute in `0_dashboard.py` so adjusting 50 SKUs only creates 1 row in the dashboard (per status) instead of 50.
- **2026-06-28**: **Fixed Navigation Timeout for Heavy Distributors:** Changed `wait_until="domcontentloaded"` to `wait_until="networkidle"` at the end of `_login` to ensure all JavaScript `actionpath` event listeners are fully attached to the DOM before clicking the module menu.
- **2026-06-28**: **Optimized Initial Login Reliability:** Changed `page.goto(URL_LOGIN)` to also use `networkidle` globally across all automation flows to guarantee stable script attachment on initial load.
- **2026-06-28**: **Fixed Streamlit Cloud OS Error:** Disabled Streamlit's `fileWatcherType` in `config.toml` to prevent `[Errno 24] inotify instance limit reached` crashes caused by the large number of temporary browser files Playwright creates.
- **2026-06-28**: **Fixed Warehouse Exception Mapping:** Added `database.get_distributor_warehouse_exceptions()` lookup to `run_execution_manual` to ensure distributors like PT. Panjunan properly use `00GOOD_WHS` instead of the hardcoded `GOOD_WHS` fallback.
- **2026-07-01**: Implemented Error Screenshots: Automation engine now captures a screenshot upon fatal error within the active browser session (`managed_browser_session`), storing it in `screenshots/` and appending the path to the exception.
- **2026-07-01**: Enhanced Telegram Notifications: `utils.py/send_telegram_alert` upgraded to support `photo_path` (using `sendPhoto` multipart/form-data upload) to send error screenshots. Modified `playwright_engine.py` exception handlers to attach the screenshot. Modified `playwright_engine.py` abort notifications to dynamically append a comma-separated list of failed SKUs.
- **2026-07-01**: Implemented Global Dry Run Mode: Added a global toggle switch in `app.py` sidebar mapped to `st.session_state.dry_run_enabled`. Updated all execution and extraction functions in `playwright_engine.py` to natively read this state. When enabled, it executes all form interactions normally but explicitly bypasses the final "Save" button click and Supabase database logging to safely test workflows.
- **2026-07-01**: **UI Enhancement:** Redesigned the "Dry Run (Simulate Only)" toggle using an ultimate Neo-Brutalist flat premium design (solid flat `#0068C9` shadow, full width `row-reverse` layout pushing the toggle to the right, and an injected CSS subtitle) to create a jaw-dropping Enterprise layout while remaining 100% compliant with the non-glassmorphism Memory UI/UX guidelines.
- **2026-07-01**: **Global Architecture:** Executed a massive global UI overhaul replacing the default Streamlit soft styling with a cohesive Neo-Brutalist Premium Execution design. Containers, buttons, alerts, text inputs, metric cards, and dashboard headers now exclusively use stark white backgrounds, thick solid `#0F172A` borders, high-contrast solid drop shadows (no blur), and physical mechanical hover transitions.
- **2026-07-02**: **UI Enhancement:** Fixed styling on Dashboard Recent Activity Timeline. Converted thin gray borders to thick `#0F172A` lines, and converted default circular icons to Neo-Brutalist solid squares with `#0068C9` block backgrounds and `#0F172A` box-shadows.
- **2026-07-02**: **New Feature:** Added `pages/7_ai_assistant.py` module to integrate DeepSeek-V4-Pro via NVIDIA API. Configured `openai` client to fetch `NVIDIA_API_KEY` from Streamlit secrets, providing a Neo-Brutalist styled Chatbot interface for data analytics. Added access module launcher to Dashboard grid.
- **2026-07-02**: **UI Enhancement:** Finalized Neo-Brutalism Global CSS. Replaced thin gray container borders with 2px solid #0F172A, changed all blue Streamlit shadows to pure black #0F172A, removed border-radius from headers/buttons/containers, and thickened table grid borders to create a cohesive, flat, high-contrast structural aesthetic.
- **2026-07-02**: **UI Enhancement:** Removed the AI Assistant module from the dashboard and sidebar per user request.
- **2026-07-02**: **UI Enhancement:** Fixed Streamlit 1.36+ `st.segmented_control` CSS DOM selectors (`stButtonGroup[role="radiogroup"]`) so Neo-Brutalism styling applies correctly. Replaced module header emojis in `utils.py` with bold SVG line icons to match the flat, high-contrast aesthetic. Adjusted Dashboard inner header heights to be compact (`line-height: 1.2`, `div` instead of `h3`). Fixed duplicate CSS rule overriding `stSelectbox` label text color to blue.
- **2026-07-02**: **UI Enhancement:** Removed Streamlit Material icon ligatures from `st.segmented_control` options in `pages/1_inventory_adjustment.py` because the CSS `uppercase` combined with `letter-spacing` caused the icons to render as overlapping raw text.
- **2026-07-02**: **UI Enhancement:** Applied global Neo-Brutalism styling to `st.file_uploader` (dropzone and secondary button) and `st.info`/`st.warning`/`st.error` (alerts) via `static/style.css` to ensure standard Streamlit components maintain the stark, high-contrast, flat design aesthetic across all pages.
- **2026-07-02**: **UI Enhancement:** Replaced the generic `st.page_link` Dashboard return button on module pages with a custom HTML/CSS Neo-Brutalist breadcrumb navigation component in `utils.py` and `static/style.css`.
- **2026-07-02**: **UI Enhancement:** Fixed styling on TERMINATE button in `playwright_engine.py` execution progress bar. Redesigned CSS selectors using adjacent sibling combinators targeting a hidden injected markdown anchor instead of brittle Streamlit column wrappers.
- **2026-07-02**: **Bug Fix (Critical):** Fixed the "Full Page Reload / Session State Loss" bug on the Breadcrumb Dashboard button. Solved by injecting a hidden, native `st.page_link` anchor and using vanilla JavaScript `onclick` in the breadcrumb HTML to synthetically trigger a click on the native Streamlit router link.
- **2026-07-02**: **Revert:** Reverted the Neo-Brutalist Breadcrumb UI navigation component back to the standard Streamlit `st.page_link` "Dashboard" button based on user request.
- **2026-07-02**: **UI Enhancement:** Fine-tuned vertical alignment on the Terminate execution status bar by injecting a matching `margin-top` to adjacent HTML columns.
- **2026-07-02**: **Bug Fix:** Fixed an issue where the Neo-Brutalist File Uploader container on mobile devices intercepted touch gestures. Applied `overflow: hidden !important` to disable internal container scrolling.
- **2026-07-02**: **UI Enhancement:** Implemented "Stealth Terminate" feature: replaced the dedicated red Terminate button with a compact stealth icon-button integrated into the PROCESSED status box header. Added a raw Javascript interceptor `confirm()` dialog to prevent accidental clicks.
- **2026-07-03**: **Performance & UI Fix:** Refactored execution loop UI rendering in `playwright_engine.py` to stop the PROCESSED button and layout from blinking/flickering. Layout and JS injection are now instantiated exactly once outside the loop.
- **2026-07-03**: **UI Enhancement:** Fixed critical layout alignment issue for the progress logger across all modules. Replaced the broken `st.columns` approach with a single responsive flex container. Injected a pure CSS neo-brutalist modal for the Terminate execution flow.
- **2026-07-04**: **UI Enhancement:** Fixed micro-alignment and dimension issues on the Terminate execution modal. Hardcoded absolute exact dimensions to the Streamlit CONFIRM button inside the modal.
- **2026-07-04**: **UI Enhancement:** Replaced the native Streamlit `@st.dialog` Sign Out popup with a custom injected pure CSS Neo-Brutalist modal matching the Terminate execution modal aesthetics.
- **2026-07-04**: **Bug Fix:** Fixed the PROCESSED execution counter (0/0 bug) in `playwright_engine.py`. Switched to `streamlit.components.v1.html()` to reliably execute DOM update scripts.
- **2026-07-04**: **Bug Fix:** Restored the missing CONFIRM buttons in Terminate and Sign Out modals. Fixed flaw in CSS sibling selector by replacing it with a compound `:has()` selector.
- **2026-07-04**: **UI Enhancement:** Completely refactored Confirm buttons in Terminate and Sign Out modals. Removed CSS `position: fixed` workarounds, injected native HTML Confirm buttons into the modal DOM structure.
- **2026-07-04**: **Bug Fix:** Fixed unresponsive HTML Confirm buttons by rewriting Javascript binding to use robust Event Delegation attached to `window.parent.document.body`.
- **2026-07-04**: **UI Enhancement:** Replaced Javascript anchor-syncing with Pure CSS Modal Layout for Terminate and Sign Out dialogs. Completely stripped all injected JavaScript and redesigned modal to use stacked Top/Bottom layout.
- **2026-07-04**: **UI Enhancement:** Fixed Dashboard "Recent Activity" timeline white space bug. Restored display count to 5 items and decoupled the right column from the global `height: 100% !important` flex-grow rule using a custom `.compact-timeline-marker` CSS class.
- **2026-07-04**: Performed GitHub repository cleanup. Removed broken git submodules (`newspage_automation`, `ponytail`, `spec-kit`), deleted junk files (`diff.txt`, `style_backup.css`, `bg_paper.png`, `scripts/refactor.py`, `ai_assisted_distributed_project_memory_system.md`, `skills.md`, `skills-lock.json`, `brain/`), and cleaned up duplicate entries in `.gitignore` and `MEMORY.md`.
- **2026-07-04**: **LOCKED DESIGN DECISION**: Neo-Brutalism is permanently locked as the sole design system for this project. Full design token specification (colors, shadows, borders, dimensions, fonts) has been documented in `MEMORY.md` under the "Locked Design System" section and enforced via `AGENTS.md`. All future UI work must strictly comply without exception.


- Installed Spec Kit community extensions (verify, verify-tasks, bugfix, checkpoint, archive) and presets (toc-navigation, explicit-task-dependencies) to enhance spec-driven workflow.
- **2026-07-04**: **New Feature (Unlocked via password "Dama"):** Added success screenshot capture to all bot workflows in `playwright_engine.py`. The bot now takes a screenshot of the browser's final state right before logout upon successful completion and sends it to Telegram alongside the success message. Affects: `run_extract`, `run_sales_extract`, `run_execution`, `run_execution_manual` (which also covers `run_mutasi_execution`). Graceful fallback to text-only alert if screenshot capture fails. Screenshots saved to `screenshots/` directory with prefix `success_*`.
- **2026-07-04**: **Feature Update (Unlocked via password "Dama"):** Refined success screenshot logic for Stock Adjustment (`run_execution` and `run_execution_manual`) in `playwright_engine.py`. Instead of capturing a blank form after saving, the bot now specifically navigates back to the List view, sets the "Date To" filter to today, sets "Status" to All, clicks Search, waits for the newly generated transaction row to appear in the grid, and takes a screenshot of the actual grid result.
- **2026-07-05**: **Bug Fix (Unlocked via password "Dama"):** Corrected the Stock Adjustment screenshot filter logic to specifically search for "Approved" (`A`) transactions instead of all statuses, matching user workflow requirements.
- **2026-07-05**: **UI Enhancement:** Updated the `make_solid_box` ("Extracted" info box) to use a solid `#0068C9` blue background with white font, and `make_success_box` ("Success" box) to use a solid `#2E7D32` green background with white font, aligning with the neo-brutalism design style.
- **2026-07-05**: **UI Enhancement:** Styled internal grid lines of `st.data_editor` to be thick solid black by injecting `--gdg-border-color` and `--gdg-line-color` CSS variables to `#0F172A` in `style.css`.
- **2026-07-05**: **UI Enhancement:** Renamed column headers in `1_inventory_adjustment.py` data editor from "Qty PAC" to "PAC", "Qty CAR" to "CAR", and "Qty EA" to "EA" per user request.

- Added an optional file uploader (CSV/Excel) to the Manual Entry feature in pages/1_inventory_adjustment.py to allow users to upload files and map columns directly into the manual entry grid, per Spec 005.
- **2026-07-05**: **Bug Fix**: Fixed a TimeoutError in playwright_engine.py during Stock Adjustment success screenshot capture by bypassing the flaky ASP.NET CalendarExtender UI and injecting the date directly into the text input.
- **2026-07-05**: **Bug Fix**: Removed the explicit wait for the grid row pag_I_StkAdj_grd_List_ctl02_grs_TXN_NO_Value in the Stock Adjustment screenshot logic to prevent a 10000ms TimeoutError. The bot now takes the screenshot immediately after the search loads.
- **2026-07-05**: **Bug Fix**: Fixed Streamlit Cloud memory limits error (OOM) by applying aggressive memory optimization flags (--disable-dev-shm-usage, --no-sandbox) to the Chromium launch options in playwright_engine.py, and explicitly calling gc.collect() upon session close.
- **2026-07-05**: **Bug Fix**: Improved Stock Adjustment screenshot logic. Instead of just screenshotting the grid, the bot now explicitly clicks the 'Sort' header twice, opens the latest generated transaction detail page, and captures the inner record view.
- **2026-07-05**: **Bug Fix**: Implemented an explicit fallback mechanism in the Stock Adjustment screenshot logic. If the transaction row is missing from the grid (timeout), the bot now captures and sends the empty List View screenshot to Telegram instead of crashing, allowing the user to debug visually.
- **2026-07-05**: **Bug Fix**: Changed Stock Adjustment screenshot filter status from Approved (A) to Pending (P) because newly created adjustments default to Open (Pending), which was causing the grid to be empty.
- **2026-07-05**: **Refactor**: Drastically simplified Stock Adjustment screenshot capture per user instructions. Removed row sorting and detail view navigation entirely. Now it only filters by Approved, clicks Search, and captures the List View.
- **2026-07-05**: **Feature**: Restored sorting and detail document navigation for Stock Adjustment screenshot capture after successfully mitigating ASP.NET PostBack race conditions on the List View filters.
- **2026-07-05**: **Project Pivot**: User explicitly cancelled the Telegram-to-WhatsApp gateway (OpenWA) deployment project due to free-tier PaaS limitations (abuse flags on HF Spaces, no free persistent volumes on Render). Workflow terminated.
- **2026-07-05**: **Security Hardening**: Implemented AES-256 Fernet session cookie encryption/decryption in `app.py` to prevent session hijacking/spoofing, and environmental parameter sanitization in `pages/0_dashboard.py` ping subprocess execution to block Remote Code Execution (RCE) via URL/credentials injection.
- **2026-07-05**: **UI Enhancement**: Migrated User Guide expanders to Neo-Brutalist `@st.dialog` modals across all 7 page modules to save vertical space and improve aesthetics.
- **2026-07-05**: **UI Enhancement**: Refactored the User Guide trigger button across all 7 modules. Replaced the bulky full-width primary button with a compact, floating Neo-Brutalist secondary icon button injected directly into the header card via CSS `:has()` combinators and negative margins to optimize layout space.
- **2026-07-05**: **Bug Fix**: Disabled the User Guide button (`disabled=st.session_state.is_bot_running`) across all 7 modules when the Playwright bot is actively executing to prevent accidental Streamlit reruns that would crash the automation thread.
- **2026-07-05**: **Feature Removal**: Removed the User Guide feature entirely from all modules per user request, including modal logic, buttons, and floating CSS, finalizing the transition to a purely automated layout interface.
- **2026-07-05**: **Constitution Update**: Amended `constitution.md` to version 2.1.0, adding Principle VII: Action Button Consolidation. This enforces that post-execution actions (like "Clear Data") MUST dynamically replace the primary execution button (like "Extract") in the exact same location to prevent UI clutter and stacking.

## Critical Knowledge: ASP.NET UpdatePanel Race Conditions (2026-07-05)
- **The Problem**: When navigating Newspage (e.g., clicking a side menu tab like List View), Playwright often moves faster than the ASP.NET AJAX PostBack. `networkidle` returns instantly because the network request hasn't started yet. Playwright then changes dropdowns/filters, but half a second later, the server returns the PostBack and OVERWRITES the DOM, resetting the filters to their defaults (e.g., Status reverting to Open (Pending)).
- **The Solution**: ALWAYS add `page.wait_for_timeout(3000)` IMMEDIATELY AFTER triggering a click that causes an ASP.NET partial postback (before interacting with the DOM). This guarantees the server request starts and finishes replacing the DOM before Playwright touches it.
- **Sorting & Details**: For List View grids, ALWAYS use `page.evaluate('__doPostBack("pag_I_StkAdj_grd_List","Sort$TXN_NO")')` for sorting instead of UI clicks. To click into a transaction detail, extract the href attribute (which is usually a javascript __doPostBack string) and execute it directly via `page.evaluate()` rather than relying on Playwright clicks which can be intercepted by loading spinners.

- [2026-07-05] Added 'Kirim ke WhatsApp' feature to forward automation screenshots locally using a headless Playwright Chromium session, bypassing the need for a PaaS backend or paid Telegram gateways.

- **2026-07-05**: **Bug Fix**: Changed screenshot saving and loading in WhatsApp forwarder to use absolute paths based on script location (__file__) to prevent file-not-found errors caused by mismatched current working directories across Streamlit sessions.
- **2026-07-05**: **Bug Fix**: Disables automatic deletion of execution success screenshots in Telegram alert callback (send_telegram_alert) by adding a delete_after parameter, allowing the WhatsApp forwarder UI to load and send it later.
- **2026-07-05**: **Feature**: Replaced the automated WhatsApp Forwarder UI section in the Inventory Adjustment module with a clean Neo-Brutalist Download Screenshot container and preview, allowing users to manually download and share the proof of transaction.
- **2026-07-05**: **UI Enhancement**: Wrapped the screenshot download preview section in a native Neo-Brutalist st.expander accordion component to save vertical layout space.
- **2026-07-05**: **UI Enhancement**: Styled the image caption to match the Neo-Brutalist design language (bold, uppercase, dark #0F172A, sans-serif) rather than the default browser styling.
- **2026-07-05**: **Constitution Update**: Amended `constitution.md` to version 2.2.0, adding Principle VIII: Mandatory Remark Input for Adjustments. This enforces that the Inventory Adjustment module MUST have a "Remark" input column matching the Stock Mutation module before execution proceeds.
- **2026-07-05**: **Constitution Patch**: Amended `constitution.md` to version 2.2.1, refining Principle VIII to require the Remark column to be automatically pre-filled with the uploaded file's name when a user uploads data via Excel/CSV, while remaining editable.
- **2026-07-05**: Added Remark column (`st.text_input`) to Inventory Adjustment module with auto-fill capabilities from file names, mirroring Stock Mutation.
- **2026-07-05**: **Constitution Update**: Amended `constitution.md` to version 2.3.0, adding Principle IX: Client-Side Sharing Delegation, prioritizing browser-native delegation for WhatsApp messaging integration.
- **2026-07-05**: **Constitution Update**: Amended `constitution.md` to version 2.4.0, adding Principle X: Streamlit Deprecation Compliance — Layout Widths. This enforces replacing deprecated `use_container_width` with the modern `width` parameter.
- **2026-07-05**: Initialized feature specification for Streamlit layout width migration under `specs/016-replace-use-container-width/spec.md`.
- **2026-07-05**: **Archival**: Archived feature `016-replace-use-container-width` (Streamlit Layout Width Deprecation Migration). Bootstrapped `.specify/memory/spec.md`, `.specify/memory/plan.md`, and `.specify/memory/changelog.md` from first feature archival. Updated feature spec/plan status to Completed. 12/12 tasks completed. Constitution compliance verified (Principle X).
- **2026-07-05**: **Feature**: Replaced the server-side WhatsApp Forwarder with a browser-delegated Client-Side WhatsApp Share button. The feature encodes the screenshot to base64, copies it to the browser clipboard using Clipboard API in the parent context, and redirects to WhatsApp Web in a new tab.
- **2026-07-05**: **UI Enhancement**: Modified the "Reporting Period" selectbox layout in the Dashboard (`pages/0_dashboard.py`) to span full width, allowing the styled title badge to center beautifully above it according to the Neo-Brutalist design.
- **2026-07-05**: **UI Enhancement**: Rearranged layout in the Inventory Adjustment module (`pages/1_inventory_adjustment.py`) so that the "Remark" input field is now positioned above the terminal execution log instead of below it.
- **2026-07-05**: **UI Enhancement**: Reverted the "Remark" input layout in both Inventory Adjustment and Stock Mutation modules. The label is now returned to the standard "Remark" (without the extra text) and centered gracefully above the full-width input field, matching the original intended Neo-Brutalist design.
- **2026-07-05**: **Feature Update**: Updated the Telegram bot finished notification text format to match new specifications: "[OK] BOT FINISHED" replaced with "STOCK ADJUSTMENT REPORT", "Dis:" with "Distributor :", "Success : | Failed :" with "Total SKU Mismatch :", and added a new line "Done by : {current_user}" identifying the Streamlit user who executed the run.
- **2026-07-05**: **Feature Update**: Added a new line "File Received : {file_name}" to the Telegram bot finished notification text format, dynamically parsing the uploaded distributor stock file or manual CSV/Excel file in the Inventory Adjustment module.
- **2026-07-05**: **DevOps**: Created `.github/workflows/keep-alive.yml` to automatically ping the Streamlit Cloud app every hour using `curl`, preventing the instance from entering sleep mode due to inactivity.
- **2026-07-05**: **UI Enhancement**: Removed redundant layout spacing between primary action buttons in the Inventory Adjustment module to tighten the Neo-Brutalist design grouping.
- **2026-07-05**: **UI Enhancement**: Extended CSS button marker classes to explicitly target and override the 'primary' Streamlit button variants, enabling color-coding (red for clear data, green for process) and resolving the 'all buttons are blue' problem.
- **2026-07-05**: **UI Enhancement**: Tightened vertical spacing in the execution progress panel (progress bar, terminate button, success/error boxes) by removing extraneous internal margins and injecting negative container margins, achieving a more cohesive Neo-Brutalist layout.
- **2026-07-05**: **Bug Fix**: Separated the WhatsApp forwarding feature into distinct 'COPY GAMBAR' and 'COPY TEKS' buttons. This circumvents a hard limitation in WhatsApp Web that automatically drops textual payload when pasted concurrently with image binary data.
- **2026-07-05**: **Bug Fix**: Fixed a critical omission in the Playwright execution engine where 'last_alert_msg' was not being persisted to the Streamlit session state during Auto Compare runs, resulting in the WhatsApp forwarder erroneously serving fallback metadata with inaccurate SKU mismatch counts.
- **2026-07-05**: **Bug Fix**: Synchronized the text template of the client-side WhatsApp fallback generator to perfectly mirror the formatting of the server-side Telegram bot alerts (including File Received and Runtime).
- **2026-07-06**: **UI Fix**: Reverted excessive negative margins on the terminate button container that caused the progress bar, terminate button, and success box to aggressively overlap and hide their shadows. Preserved the double-margin fix in utils.py.
- **2026-07-06**: **Documentation**: Created `product_requirements_document.md` as the root PRD required by `AGENTS.md`, documenting product goals, module scope, security, integrations, locked logic, and acceptance criteria.
- **2026-07-06**: **Spec Kit Bugfix**: Resolved `BUG-001` / analyze finding `C1` for `017-mobile-responsive` by marking deprecated `use_container_width=True` as disallowed in the spec assumption and confirming `width` parameter compliance in plan/tasks.
- **2026-07-06**: **Spec Kit Implementation**: Completed `/speckit-implement` for `017-mobile-responsive` by routing Dashboard full activity rows and Inventory Adjustment upload preview through `render_responsive_dataframe()`, and tightening mobile button/input sizing in `static/style.css` without changing frozen automation logic.
- **2026-07-06**: **Spec Kit Analysis Cleanup**: Resolved `/speckit-analyze` findings for `017-mobile-responsive` by aligning the wide-table acceptance scenario with stacked mobile cards and expanding T010 to cover orientation changes plus third-party embed applicability.
- **2026-07-06**: **Spec Kit Archive**: Archived `017-mobile-responsive` into `.specify/memory/spec.md`, `.specify/memory/plan.md`, and `.specify/memory/changelog.md`; updated `AGENTS.md` to point future agents at the consolidated Spec Kit plan.
- **2026-07-06**: **Spec Kit Agent Context**: Refreshed the managed Spec Kit block in `AGENTS.md` via `speckit-agent-context-update`, explicitly pointing it at `.specify/memory/plan.md`.
- **2026-07-06**: **Security Audit Task A**: Completed a read-only security audit and created `SECURITY_AUDIT_REPORT.md`, covering secrets, sessions, credential fallback, unsafe HTML, uploads, subprocess use, dependency CVE readiness, Supabase/RLS evidence, screenshots, and error exposure without changing frozen business logic.
- **2026-07-06**: **Regression Task B**: Added `tests/manual/REGRESSION_CHECKLIST.md`, a repeatable manual smoke/regression checklist for Dashboard, login/session, frozen workflows, credential auto-encryption, uploads, logging, and responsive UI without modifying frozen business logic.
- **2026-07-06**: **Database Migration Task C**: Added `docs/database_migrations.md`, documenting Supabase table usage, baseline schema SQL, additive migrations for `run_by`, `extraction_history.status`, and `uploaded_files`, plus an RLS review checklist without changing application logic.
- **2026-07-06**: **Error Taxonomy Task D**: Added `error_taxonomy.py` and `docs/error_taxonomy.md` to define safe user-facing error codes, categories, severities, and logging patterns without rewiring frozen automation flows.
- **2026-07-06**: **Smoke Test Task E**: Added `tests/smoke/` unittest coverage for numeric parsing, SKU cleanup, comparison output, responsive dataframe HTML escaping, and error taxonomy formatting without calling Supabase, Telegram, Playwright, or frozen automation workflows.
- **2026-07-06**: **Smoke Test CI**: Added `.github/workflows/smoke-tests.yml` to run the lightweight `tests/smoke` unittest suite automatically on pushes, pull requests, and manual workflow dispatch without executing Playwright or Newspage workflows.
- **2026-07-06**: **Auth Session Smoke Coverage**: Added smoke tests for login lockout, expired lockout reset, and max-attempt lockout behavior using a fake Supabase client, keeping tests offline and leaving Playwright/Newspage logic untouched.
- **2026-07-06**: **Dashboard Log Smoke Coverage**: Added `dashboard_log_utils.py` plus smoke tests for dashboard module detection, deduplication, sorting, and Jakarta timestamp formatting without importing the Streamlit dashboard page or calling Supabase.
- **2026-07-06**: **Upload Parsing Smoke Coverage**: Extended smoke tests for `load_data()` CSV comma fallback and ZIP `INVT_MASTER` CSV selection using in-memory files, without changing page upload behavior or Playwright flows.
- **2026-07-06**: **Utility Smoke Coverage**: Extended smoke tests for shared `utils.py` URL parameter encoding/decoding and terminal logger HTML escaping, keeping coverage offline and avoiding any Playwright or Supabase calls.
- **2026-07-06**: **Smoke Edge Cases**: Extended smoke coverage for `clean_html()`, `render_metric_card()` escaping, and dashboard cutoff filtering, bringing the lightweight offline suite to 19 passing tests without touching Playwright or Newspage logic.
- **2026-07-06**: **Database Helper Smoke Coverage**: Extended smoke tests for `database.py` system config casting, SKU rule loading, multiplier filtering, and distributor warehouse exception mapping using the fake Supabase client, bringing the offline suite to 23 passing tests.
- **2026-07-07**: **Distributor Vault Smoke Coverage**: Extended offline smoke tests for `database.py` distributor list fallback, encrypted credential reads, and plain-text vault auto-encryption updates using the fake Supabase client, without touching frozen Playwright or Newspage logic.
- **2026-07-07**: **Database Auth/Logging Smoke Coverage**: Extended offline smoke tests for `database.py` bcrypt auth checks, extraction/adjustment log payloads, encrypted-password failure fallback, and login reset behavior using the fake Supabase client, keeping coverage away from Playwright and frozen Newspage flows.
- **2026-07-07**: **Secrets Bootstrap Smoke Coverage**: Extended offline smoke tests for `database.py` environment-based Supabase initialization caching, encryption-key fallback, and empty encrypt/decrypt guards to reinforce the security baseline without touching production automation paths.
- **2026-07-07**: **Compare/UI Helper Smoke Coverage**: Extended offline smoke tests for distributor warehouse filtering, multiplier-based comparison matching, alert box escaping, neo-table escaping/float formatting, and distributor URL resolver behavior without invoking Streamlit pages or Playwright.
- **2026-07-07**: **Session/Upload Fallback Smoke Coverage**: Extended offline smoke tests for Streamlit session-state initialization, encoded/plain date URL resolution, unsupported upload fallback, and ZIP-without-CSV fallback using in-memory files only.
- **2026-07-07**: **Production Readiness Gate Completion**: Added `scripts/production_readiness_audit.py`, smoke coverage for the audit gate, `.github/workflows/security-audit.yml`, `docs/spec_artifact_policy.md`, and `docs/production_readiness_status.md`. Ran local production-readiness audit, smoke suite, and `pip-audit`; remediated fixable dependency CVEs by bumping pinned package versions and documented the remaining `torch` CVE-2025-3000 exception because `pip-audit` reports no fix version.
- **2026-07-07**: **Security Audit CI Fix**: Verified the GitHub `Security Audit` workflow remotely and found the dependency scan job passed while the static job failed at smoke tests due to missing smoke dependency installation. Added the same `pandas==3.0.2` install step used by the dedicated smoke workflow.
- **2026-07-07**: **Supabase Schema Check Gate**: Added `scripts/supabase_schema_check.py` and smoke coverage for required Supabase tables/columns, including `run_by`, `extraction_history.status`, and `uploaded_files.file_content_base64`, enabling read-only live schema verification without printing secrets or row data.
- **2026-07-07**: **Supabase Live Schema Finding**: Ran the read-only live Supabase schema check. All required tables/columns passed except `uploaded_files`, which is missing from the live schema cache. Documented the result and exact migration SQL in `docs/supabase_live_schema_check_2026-07-07.md`.
- **2026-07-07**: **Dependency/Handoff Follow-up**: Documented dependency pruning evidence and strategy in `docs/dependency_pruning_review.md`, noting no direct app imports for the heavy ML/agent stack tied to the `torch` CVE exception. Added `docs/antigravity_handoff.md` with continuation commands and the remaining Supabase `uploaded_files` migration action.
- **2026-07-06**: **Spec Kit Specify**: Created and quality-validated `018-security-audit` specification for Audit Keamanan Sistem, focusing on audit reporting, credential/session protection, input safety, and remediation tracking without changing frozen business logic.
- **2026-07-07**: **Configuration Cleanup**: Removed unused `Claude-Mem` rule from `AGENTS.md` per user request since it does not apply to Antigravity (Gemini).
- **2026-07-07**: **Supabase Live Schema Gap Resolved**: Verified creation of `uploaded_files` table in live Supabase database via `scripts/supabase_schema_check.py`, confirming all 10 required tables and columns now pass schema verification. Updated production readiness status and live schema check docs.
- **2026-07-07**: **Dependency Pruning Merged**: Created branch `chore/dependency-pruning` and removed 32 unused heavy ML/agent packages (`torch`, `sentence-transformers`, `transformers`, `faiss-cpu`, `langchain*`, `langgraph*`, `langsmith`, `reflex*`, `firebase_admin`) from `requirements.txt`. Verified that `production_readiness_audit.py` (PASS), 48 smoke tests (OK), and `pip-audit` (0 known vulnerabilities found, resolving `CVE-2025-3000`) all pass clean. Verified Playwright automation via live staging regression testing (PASS) on Streamlit Cloud, and merged into `main`.
- **2026-07-07**: **Error Taxonomy Runtime Adoption**: Created branch `chore/error-taxonomy-wiring` and integrated `error_taxonomy.py` (`format_user_error`, `format_log_error`) into non-frozen runtime support paths (`app.py`, `utils.py`, `pages/0_dashboard.py`). Standardized UI error codes for login authentication (`[AUTH-001]`, `[AUTH-002]`), session timeout toasts (`[SESSION-001]`), Telegram notification failures (`[NOTIFY-001]`), dashboard log loading (`[DB-001]`), and superuser ping tests (`[CONFIG-001]`, `[AUTO-001]`, `[AUTO-002]`, `[UNK-001]`) without exposing raw exception strings or stack traces, while keeping frozen business logic modules untouched per Freeze Rule. Updated `docs/error_taxonomy.md`, `REGRESSION_CHECKLIST.md`, and `CHANGELOG.md` accordingly.
- **2026-07-07**: **Smoke Test Extension**: Added `tests/smoke/test_extended_offline_smoke.py` covering data processing (`load_data` Excel/CSV-ZIP, `clean_sku_column`, `process_compare` filtering and multipliers), error taxonomy structure and formatting, and UI helper functions (`render_indicators`, `render_header`, `render_footer`, `render_terminal`, `make_solid_box`) with Neo-Brutalist markup assertions. Brought offline smoke test suite to 57 passing tests without calling Supabase, Telegram, Playwright, or frozen automation workflows.
- **2026-07-07**: **Option 1 Completion (Error Taxonomy in UI Pages)**: Extended adoption of `error_taxonomy.py` across all remaining UI modules (`pages/1_inventory_adjustment.py` through `pages/6_initial_stock.py`) replacing raw `st.error()` calls with `format_user_error()`, and added smoke test verification (`test_all_ui_pages_use_error_taxonomy`), bringing offline smoke tests to 58 passing tests.
- **2026-07-07**: **Option 2 Completion (RLS & Index Automated Check Script)**: Developed `scripts/supabase_rls_index_check.py` and smoke tests (`tests/smoke/test_supabase_rls_index_check.py`) to automatically validate Row Level Security (RLS) activation status and index integrity for all 10 required production tables via Supabase REST API (RPC) read-only, with `--print-sql` support for one-time SQL setup. Brought offline smoke test suite to 63 passing tests and verified clean audit baseline.
- **2026-07-07**: **Option 3 Completion (Security Hardening & Spec Kit 018 Verification)**: Executed official Spec-Kit workflow (`/speckit-implement`, `/speckit-verify-tasks-run`, `/speckit-verify-run`) for `018-security-audit`. Standardized raw login lockout and authentication failure alerts in `app.py` to Neo-Brutalist containers (`border: 3px solid #0F172A`, `box-shadow: 6px 6px 0px 0px #0F172A`) using error taxonomy codes `[AUTH-001]` and `[AUTH-002]`. Built dedicated automated regression suite `tests/smoke/test_security_audit_smoke.py` covering US1 (CVE scanning), US2 (Fernet encryption roundtrip, tampering resistance, lockout thresholds), and US3 (HTML escaping against XSS, subprocess safety without shell=True). Enhanced `FakeFernet` cipher mock in `test_auth_session_smoke.py`. Verified all 14 tasks in `018-security-audit/tasks.md` and generated `specs/018-security-audit/verify-tasks-report.md`. Brought offline smoke test suite to 68 passing tests without touching frozen business logic.
- **2026-07-07**: **Option 4 Completion (Live Integration / Regression Testing)**: Performed comprehensive live integration and regression testing against production database and automation gates. Executed live Supabase schema verification (`scripts/supabase_schema_check.py` - 10/10 tables pass), live RLS check (`scripts/supabase_rls_index_check.py`), live distributor credential decryption check (`scripts/check_invalid_creds.py` - 100% stored credentials decrypt cleanly), live auto-encryption workflow check (`scripts/test_auto_encrypt.py` - round-trip plaintext insertion, auto-encrypt on fetch, and cleanup pass), production readiness audit gate (`scripts/production_readiness_audit.py` - 21/21 rules pass), and full regression test suite (`tests/smoke/test_*.py` - 68/68 tests pass in 2.637s), confirming zero regressions and full compliance with frozen business logic and Neo-Brutalist design rules.
- **2026-07-07**: **Option 5 Completion (Final Production Release Hand-off)**: Completed the step-by-step execution plan from Option 1 to Option 5. Updated final hand-off documentation (`docs/antigravity_handoff.md`, `docs/production_readiness_status.md`) with 100% verified status across static safety rules, live database checks, automated regression test suites (68/68 passing), error taxonomy adoption across all UI modules, and dependency pruning (0 known vulnerabilities). Confirmed branch `chore/error-taxonomy-expansion` is clean and ready for production deployment to Streamlit Cloud.
- **2026-07-07**: **Constitution Amendment v2.6.0 & Streamlit Cloud Build Fix**: Executed `/speckit-constitution` to amend the project constitution to v2.6.0, adding **Principle XII: Minimal & Clean Dependency Architecture (No Unused Bloat)**. Resolved Streamlit Cloud deployment crashes caused by Linux platform incompatibility (`pywin32==312` and `win32_setctime` are Windows-only) and Python runtime restrictions (`litellm==1.86.2` requiring Python >=3.10). Performed a comprehensive second round of dependency pruning, removing 77 unused heavy AI/ML/cloud and OS-specific packages (`pywin32`, `win32_setctime`, `litellm`, `mcp`, `fastapi`, `google*`, `openai`, `opentelemetry-*`, `tree-sitter*`, `volcengine*`, `grpcio*`, `protobuf`, etc.) from `requirements.txt` (reducing from 243 down to 166 cross-platform requirements). Verified 68/68 smoke tests pass and 21/21 production readiness audit rules pass on `main`.
- **2026-07-08**: **Live Deployment & Supabase RLS/Index Verification**: Verified `https://newspage.streamlit.app` live deployment health, login, dashboard, and authenticated Inventory Adjustment routing without executing mutating workflows. Installed the live Supabase `verify_rls_and_indexes()` inspection RPC and confirmed all 10 required tables pass RLS and index presence checks via `scripts/supabase_rls_index_check.py`.
- **2026-07-08**: **Session Invalidation on Password Rotation**: Implemented Spec Kit feature `019-session-invalidation` so remembered login cookies carry encrypted structured session metadata and are validated against `users_auth.session_version` before auto-login. Legacy username-only cookies and stale versions now fail closed, while successful password login bootstraps session metadata when available. Added smoke coverage and migration docs for `session_version` / `password_changed_at` without touching frozen Newspage automation workflows.
- **2026-07-08**: **Session Metadata Persistence Hardening**: Hardened remembered-login bootstrap so `ensure_user_session_version()` only returns a new version after it is persisted and readable from Supabase, preventing non-persisted metadata from producing an invalid remembered-login cookie. Added offline smoke coverage for the fail-closed case.
- **2026-07-08**: **BUG-001 Active Session Invalidation**: Patched `019-session-invalidation` so active Streamlit sessions store `current_session_version` and revalidate it against live Supabase metadata on rerun. Password rotation now clears already logged-in tabs as well as stale remembered cookies, with smoke coverage and Spec-Kit bug artifacts added.
- **2026-07-08**: **Live Active Session Invalidation Verified**: After Streamlit Cloud reboot, verified production behavior for BUG-001 with the `rizki` test account: login reached an authenticated Streamlit session, live Supabase `session_version` was rotated, and reloading the same tab returned to the sign-in form.
- **2026-07-08**: **CI Security/Smoke Follow-up**: Replaced fragile CI smoke dependency setup by installing `openpyxl` alongside `pandas`, made `test_security_audit_smoke.py` self-contained with a local Fernet stub, and pruned unused vulnerable direct requirements (`ecdsa`, `python-jose`, `PyPDF2`). Verified 77/77 smoke tests, production readiness audit, `pip-audit` with no known vulnerabilities, and latest GitHub Actions `Smoke Tests` / `Security Audit` success on commit `aaa9d00`.
- **2026-07-08**: **Workspace Hygiene Hardening**: Removed local root-level scratch/debug Python files, added `.pytest_cache/` to `.gitignore`, and extended `scripts/production_readiness_audit.py` with a `root-debug-artifacts` gate so scratch/test/debug Python files at repository root fail readiness checks before they can clutter or destabilize the workspace.
- **2026-07-08**: **Release Readiness Gate**: Added a repeatable broad-release gate covering release candidate inputs, local/CI checks, live Streamlit Cloud smoke, session/credential validation, manual regression scope, and rollback criteria in `docs/release_readiness_checklist.md`. Added the manual GitHub Actions `Release Readiness` workflow and wired the production readiness audit/smoke suite to require the checklist.
- **2026-07-08**: **Release Gate Verified**: Verified release hardening commit `d19fb34` with local production readiness audit, local smoke suite (78/78), live Streamlit Cloud `/healthz`, live Supabase schema/RLS/index checks, and GitHub Actions `Smoke Tests` / `Security Audit` success. The manual `Release Readiness` workflow is available for release-owner dispatch after Streamlit Cloud deploys the target commit.
- **2026-07-08**: **Release Gate Automation**: Upgraded `.github/workflows/release-readiness.yml` from manual-only to automatic on `main` pushes and pull requests, while retaining manual dispatch. Added a `Live Health Probe` job for `main` pushes that validates `https://newspage.streamlit.app/healthz` after the offline release gate passes.


